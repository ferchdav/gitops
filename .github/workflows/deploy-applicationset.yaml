name: Validate and Deploy ApplicationSet

on:
  pull_request:
    branches:
      - main
    paths:
      - '*/*/applicationset.yaml'

  push:
    branches:
      - dev
      - main
    paths:
      - '*/*/applicationset.yaml'

  workflow_dispatch:

permissions:
  contents: read

jobs:
  # =========================================================
  # VALIDATION
  # =========================================================
  validate:
    name: Validate ApplicationSet
    runs-on: ubuntu-latest

    steps:
      # -----------------------------------------------------
      # Checkout repository
      # -----------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -----------------------------------------------------
      # Install validation dependencies
      # -----------------------------------------------------
      - name: Install YAML validation tools
        run: |
          python -m pip install --upgrade pip
          pip install yamllint pyyaml

      # -----------------------------------------------------
      # Find changed ApplicationSet files
      # -----------------------------------------------------
      - name: Find ApplicationSet files
        run: |
          set -e

          echo "GitHub event: ${{ github.event_name }}"

          # Pull Request
          if [ "${{ github.event_name }}" = "pull_request" ]; then

            BASE_SHA="${{ github.event.pull_request.base.sha }}"
            HEAD_SHA="${{ github.event.pull_request.head.sha }}"

            echo "PR base: ${BASE_SHA}"
            echo "PR head: ${HEAD_SHA}"

            git diff --name-only --diff-filter=ACMR "${BASE_SHA}" "${HEAD_SHA}" -- '*/*/applicationset.yaml' > /tmp/appset-files.txt

          # Push to main
          elif [ "${{ github.event_name }}" = "push" ]; then

            BASE_SHA="${{ github.event.before }}"
            HEAD_SHA="${{ github.sha }}"

            echo "Push base: ${BASE_SHA}"
            echo "Push head: ${HEAD_SHA}"

            # github.event.before can be all zeros on the first push
            if [ "${BASE_SHA}" = "0000000000000000000000000000000000000000" ]; then
              find . -mindepth 3 -maxdepth 3 -type f -name applicationset.yaml | sed 's#^\./##' | sort > /tmp/appset-files.txt
            else
              git diff --name-only --diff-filter=ACMR "${BASE_SHA}" "${HEAD_SHA}" -- '*/*/applicationset.yaml' > /tmp/appset-files.txt
            fi

          # workflow_dispatch
          else
            find . -mindepth 3 -maxdepth 3 -type f -name applicationset.yaml | sed 's#^\./##' | sort > /tmp/appset-files.txt
          fi

          echo
          echo "ApplicationSet files:"
          echo "------------------------------------------"

          cat /tmp/appset-files.txt

          echo "------------------------------------------"

          if [ ! -s /tmp/appset-files.txt ]; then
            echo "No ApplicationSet files found."
            exit 0
          fi

      # -----------------------------------------------------
      # Validate YAML syntax / formatting
      # -----------------------------------------------------
      - name: Validate YAML syntax
        run: |
          set -e

          while read -r file; do

            [ -z "$file" ] && continue

            echo
            echo "=========================================="
            echo "Validating YAML: $file"
            echo "=========================================="

            yamllint \
              -d '{
                extends: default,
                rules: {
                  line-length: disable,
                  document-start: disable,
                  truthy: disable
                }
              }' \
              "$file"

          done < /tmp/appset-files.txt

      # -----------------------------------------------------
      # Validate basic ApplicationSet structure
      # -----------------------------------------------------
      - name: Validate ApplicationSet structure
        run: |
          python <<'PY'
          import sys
          import yaml

          errors = []

          with open("/tmp/appset-files.txt") as files:
              filenames = [
                  line.strip()
                  for line in files
                  if line.strip()
              ]

          for filename in filenames:

              print()
              print("=" * 60)
              print(f"Validating: {filename}")
              print("=" * 60)

              try:
                  with open(filename) as f:
                      manifest = yaml.safe_load(f)

              except Exception as exc:
                  errors.append(
                      f"{filename}: YAML parsing error: {exc}"
                  )
                  continue

              if not isinstance(manifest, dict):
                  errors.append(
                      f"{filename}: YAML document must contain an object"
                  )
                  continue

              # apiVersion
              if manifest.get("apiVersion") != "argoproj.io/v1alpha1":
                  errors.append(
                      f"{filename}: apiVersion must be "
                      f"'argoproj.io/v1alpha1'"
                  )

              # kind
              if manifest.get("kind") != "ApplicationSet":
                  errors.append(
                      f"{filename}: kind must be 'ApplicationSet'"
                  )

              # metadata
              metadata = manifest.get("metadata")

              if not isinstance(metadata, dict):
                  errors.append(
                      f"{filename}: metadata is required"
                  )
              else:
                  if not metadata.get("name"):
                      errors.append(
                          f"{filename}: metadata.name is required"
                      )

              # spec
              spec = manifest.get("spec")

              if not isinstance(spec, dict):
                  errors.append(
                      f"{filename}: spec is required"
                  )
                  continue

              # generators
              generators = spec.get("generators")

              if not generators:
                  errors.append(
                      f"{filename}: spec.generators is required"
                  )

              # template
              template = spec.get("template")

              if not isinstance(template, dict):
                  errors.append(
                      f"{filename}: spec.template is required"
                  )
                  continue

              template_spec = template.get("spec")

              if not isinstance(template_spec, dict):
                  errors.append(
                      f"{filename}: spec.template.spec is required"
                  )

          if errors:

              print()
              print("==========================================")
              print("APPLICATIONSET STRUCTURE VALIDATION FAILED")
              print("==========================================")

              for error in errors:
                  print(f"ERROR: {error}")

              sys.exit(1)

          print()
          print("ApplicationSet structure validation passed.")

          PY

      # -----------------------------------------------------
      # Validate:
      #
      # <tribe>/<environment>/applicationset.yaml
      #
      # tribe MUST equal:
      #
      # spec.template.spec.project
      #
      # Example:
      #
      # ige/dev/applicationset.yaml
      #              project: ige
      #
      # dfa/prod/applicationset.yaml
      #              project: dfa
      # -----------------------------------------------------
      - name: Validate tribe matches Argo CD project
        run: |
          python <<'PY'
          import sys
          import yaml
          from pathlib import Path

          errors = []

          with open("/tmp/appset-files.txt") as files:
              filenames = [
                  line.strip()
                  for line in files
                  if line.strip()
              ]

          for filename in filenames:

              path = Path(filename)

              print()
              print("=" * 60)
              print(f"Checking tribe/project: {filename}")
              print("=" * 60)

              # Expected:
              #
              # tribe/environment/applicationset.yaml

              if len(path.parts) != 3:
                  errors.append(
                      f"{filename}: invalid path. Expected "
                      f"<tribe>/<environment>/applicationset.yaml"
                  )
                  continue

              tribe = path.parts[0]
              environment = path.parts[1]

              print(f"Tribe:       {tribe}")
              print(f"Environment: {environment}")

              try:
                  with open(filename) as f:
                      manifest = yaml.safe_load(f)

              except Exception as exc:
                  errors.append(
                      f"{filename}: cannot parse YAML: {exc}"
                  )
                  continue

              try:
                  project = (
                      manifest["spec"]
                              ["template"]
                              ["spec"]
                              ["project"]
                  )

              except (KeyError, TypeError):

                  errors.append(
                      f"{filename}: missing "
                      f"spec.template.spec.project"
                  )

                  continue

              print(f"Project:     {project}")

              if project != tribe:

                  errors.append(
                      f"{filename}: Argo CD project "
                      f"'{project}' does not match "
                      f"tribe '{tribe}'"
                  )

                  print("RESULT: FAIL")

              else:

                  print("RESULT: PASS")
                  print(
                      f"Tribe '{tribe}' matches "
                      f"Argo CD project '{project}'"
                  )

          if errors:

              print()
              print("==========================================")
              print("TRIBE / PROJECT VALIDATION FAILED")
              print("==========================================")

              for error in errors:
                  print(f"ERROR: {error}")

              sys.exit(1)

          print()
          print("All tribe/project validations passed.")

          PY

  # # =========================================================
  # # DEPLOYMENT
  # # =========================================================
  # deploy:
  #   name: Deploy ApplicationSet
  #   runs-on: ubuntu-latest

  #   needs:
  #     - validate

  #   # Deploy ONLY after merge/push to main.
  #   # Never deploy from a Pull Request.
  #   if: |
  #     github.event_name == 'push' &&
  #     github.ref == 'refs/heads/main'

  #   steps:
  #     # -----------------------------------------------------
  #     # Checkout
  #     # -----------------------------------------------------
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     # -----------------------------------------------------
  #     # Configure Kubernetes authentication
  #     #
  #     # GitHub Secret:
  #     #
  #     # KUBECONFIG_B64
  #     #
  #     # -----------------------------------------------------
  #     - name: Configure Kubernetes
  #       env:
  #         KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}
  #       run: |
  #         set -e

  #         mkdir -p ~/.kube

  #         echo "$KUBECONFIG_B64" \
  #           | base64 --decode \
  #           > ~/.kube/config

  #         chmod 600 ~/.kube/config

  #     # -----------------------------------------------------
  #     # Verify Kubernetes connectivity
  #     # -----------------------------------------------------
  #     - name: Verify Kubernetes connection
  #       run: |
  #         kubectl cluster-info

  #         echo
  #         echo "Current context:"
  #         kubectl config current-context

  #     # -----------------------------------------------------
  #     # Make sure ApplicationSet CRD exists
  #     # -----------------------------------------------------
  #     - name: Verify ApplicationSet CRD
  #       run: |
  #         kubectl get crd applicationsets.argoproj.io

  #     # -----------------------------------------------------
  #     # Find ApplicationSets changed by this push
  #     # -----------------------------------------------------
  #     - name: Find changed ApplicationSets
  #       run: |
  #         set -e

  #         BASE_SHA="${{ github.event.before }}"
  #         HEAD_SHA="${{ github.sha }}"

  #         echo "Base SHA: ${BASE_SHA}"
  #         echo "Head SHA: ${HEAD_SHA}"

  #         if [ "${BASE_SHA}" = "0000000000000000000000000000000000000000" ]; then

  #           find . \
  #             -mindepth 3 \
  #             -maxdepth 3 \
  #             -type f \
  #             -name applicationset.yaml \
  #             | sed 's#^\./##' \
  #             | sort \
  #             > /tmp/appset-files.txt

  #         else

  #           git diff \
  #             --name-only \
  #             --diff-filter=ACMR \
  #             "${BASE_SHA}" \
  #             "${HEAD_SHA}" \
  #             -- '*/*/applicationset.yaml' \
  #             > /tmp/appset-files.txt

  #         fi

  #         echo
  #         echo "ApplicationSets to deploy:"
  #         echo "------------------------------------------"

  #         cat /tmp/appset-files.txt

  #         echo "------------------------------------------"

  #     # -----------------------------------------------------
  #     # Kubernetes + ApplicationSet CRD validation
  #     #
  #     # Nothing is actually created here.
  #     # -----------------------------------------------------
  #     - name: Server-side ApplicationSet validation
  #       run: |
  #         set -e

  #         while read -r file; do

  #           [ -z "$file" ] && continue

  #           echo
  #           echo "=========================================="
  #           echo "Server-side validating: $file"
  #           echo "=========================================="

  #           kubectl apply \
  #             --dry-run=server \
  #             -f "$file"

  #         done < /tmp/appset-files.txt

  #     # -----------------------------------------------------
  #     # Deploy ApplicationSets
  #     # -----------------------------------------------------
  #     - name: Deploy ApplicationSets
  #       run: |
  #         set -e

  #         while read -r file; do

  #           [ -z "$file" ] && continue

  #           echo
  #           echo "=========================================="
  #           echo "Deploying: $file"
  #           echo "=========================================="

  #           kubectl apply \
  #             -f "$file"

  #         done < /tmp/appset-files.txt

  #     # -----------------------------------------------------
  #     # Show resulting ApplicationSets
  #     # -----------------------------------------------------
  #     - name: Show deployed ApplicationSets
  #       run: |
  #         kubectl get applicationsets \
  #           -n argocd
